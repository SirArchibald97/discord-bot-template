// Put important credentials and other values in here, such as tokens, API keys, etc.
// These values are exported into your index.js file and attached to the client object under client.config so they can be passed through to any other file

module.exports = {
    token: "" // the special token your bot uses to login to Discord
}